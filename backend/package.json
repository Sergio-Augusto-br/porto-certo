{
  "name": "porto-certo-api",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "node --watch src/server.js",
    "start": "node src/server.js",
    "db:schema": "set -a; . ./.env; set +a; psql \"$DATABASE_URL\" -f db/schema.sql",
    "db:seed": "set -a; . ./.env; set +a; psql \"$DATABASE_URL\" -f db/seed.sql"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.4.7",
    "express": "^4.21.2",
    "pg": "^8.13.1"
  }
}
